! Key codes and keysyms must be replaced with the actual
! codes of your keyboard. To search for them use xev command.
!
! Caps Lock and Escape swapping has best results using:
!
!   setxkbmap -option caps:swapescape
!
! This is because changes made by xmodmap are only applied to
! terminals, not to GUI applications.

! Makes Menu key act like Win key.
keycode 135 = 0xffeb

! Makes AltGr + A write a slash ('/' = keysym 0x002f).
!                           Changed this ↓
keycode 38 = 0x0061 0x0041 0x0061 0x0041 0x002f 0x00c6 0x00e6

!  Makes AltGr + B write a backslash ('\' = keysym 0x005c).
!                               Changed this ↓
keycode 56 = 0x0062, 0x0042, 0x0062, 0x0042, 0x005c, 0x0ad1, 0x0ad3, 0x0ad1, 0x0062, 0x0042, 0x0ad3, 0x0ad1
